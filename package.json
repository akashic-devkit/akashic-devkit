{
  "name": "akashic-devkit",
  "private": true,
  "lint-staged": {
    "**/*.{js,ts,tsx}": [
      "eslint --fix"
    ]
  },
  "type": "module",
  "scripts": {
    "dev:all": "turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "dev:cli": "turbo run dev --filter=@akashic-devkit/cli",
    "build:all": "turbo run build",
    "build:web": "turbo run build --filter=web",
    "build:cli": "turbo run build --filter=@akashic-devkit/cli",
    "deploy:cli": "turbo run deploy --filter=@akashic-devkit/cli",
    "check-types:all": "turbo run check-types",
    "lint:all": "turbo run lint",
    "lint:fix:all": "turbo run lint:fix",
    "format:all": "prettier --write \"**/*.{ts,tsx,md}\"",
    "fix:all": "turbo run fix:all",
    "cli": "cd packages/cli && pnpm cli",
    "prepare": "husky",
    "bootstrap": "pnpm prepare && pnpm install",
    "create:component": "cd apps/web && pnpm create:component",
    "create:hook": "cd apps/web && pnpm create:hook"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@commitlint/format": "^20.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.4",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
